Import( 'env' )
Import( 'compmap' )
import profiles

profiles.DynamicLibrary(
        'rpHCP_BulkCollector',
        profiles.GitInfo(),
        LIBS = 'm'
        ).Target(
            env,
            Glob( '*.c' ),
            compmap,
            'rpHostCommonPlatformIFaceLib',
            'cryptoLib',
            'networkLib',
            'Os',
            'libRestOutput',
            'kernelAcquisitionLib',
            )


# EOF
